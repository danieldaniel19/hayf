import SwiftUI

enum HAYFColor {
    static let primary = Color(red: 15 / 255, green: 15 / 255, blue: 15 / 255)
    static let secondary = Color(red: 42 / 255, green: 42 / 255, blue: 42 / 255)
    static let muted = Color(red: 96 / 255, green: 96 / 255, blue: 96 / 255)
    static let neutral = Color(red: 250 / 255, green: 250 / 255, blue: 248 / 255)
    static let surface = Color.white
    static let border = Color(red: 224 / 255, green: 224 / 255, blue: 224 / 255)
    static let orange = Color(red: 250 / 255, green: 91 / 255, blue: 28 / 255)
}
