{
  "name": "@hayf/training-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "mkdir -p dist/src/knowledge && cp -R src/knowledge/packs dist/src/knowledge/",
    "check": "tsc --noEmit",
    "dev": "langgraph dev",
    "serve": "tsx src/server.ts",
    "start": "node dist/src/server.js",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@langchain/core": "^1.2.1",
    "@langchain/langgraph": "^1.4.7",
    "@langchain/openai": "^1.5.3",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@langchain/langgraph-cli": "^1.4.2",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
